{
  "name": "benditobot",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "axios": "^0.18.0",
    "date-fns": "^1.29.0",
    "firebase-admin": "^6.3.0",
    "firebase-functions": "^2.1.0",
    "lodash": "^4.17.11",
    "telegram-typings": "^4.0.0-beta"
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/lodash": "^4.14.118",
    "@types/mocha": "^5.2.5",
    "chai": "^4.2.0",
    "concurrently": "^4.1.0",
    "firebase-tools": "^6.1.1",
    "mocha": "^5.2.0",
    "mock-cloud-firestore": "^0.9.0",
    "ngrok": "^3.1.0",
    "ts-node": "^7.0.1",
    "tslint": "^5.11.0",
    "typescript": "^3.2.1"
  },
  "scripts": {
    "lint": "tslint --project tsconfig.json",
    "copy-statics": "cp package.json dist/package.json",
    "build": "tsc",
    "build:watch": "tsc -w",
    "serve:firebase": "firebase serve --only functions",
    "serve": "concurrently \"npm:build:watch\" \"npm run serve:firebase\"",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "mocha --compilers ts:ts-node/register ./test/*.spec.ts",
    "test:watch": "mocha --compilers ts:ts-node/register ./test/*.spec.ts -w",
    "ngrok": "ngrok http 5000"
  },
  "author": "Willy Ovalle <willyovalle16@gmail.com>",
  "license": "ISC",
  "engines": {
    "node": "8"
  }
}
